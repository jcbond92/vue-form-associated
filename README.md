# Form Associated Vue Custom Elements

This is an example project of how to setup `formAssociated` use `setFormValue()` in a Vue form component that has been wrapped as a custom element.

## Running Locally
- `npm i`
- `npm run dev`
